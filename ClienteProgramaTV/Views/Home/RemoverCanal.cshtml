@model IEnumerable<ClienteProgramaTV.programacaotv.Canal>

@{
    ViewBag.Title = "RemoverCanal";
}

<h2>RemoverCanal</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Apagar |", "DeleteCanal", new { id = item.Id })
            @Html.ActionLink("Inserir Programa |", "CadastrarPrograma", new { id = item.Id })
            @Html.ActionLink("Programação", "TodaProgramacaoIdCanal", new { id = item.Id })

        </td>
    </tr>
}

</table>
